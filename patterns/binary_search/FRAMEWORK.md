- Begin
  |
  |
  if (we can not split elements into good and bad) -> Other shit
  |
  |
  elif (In the response we need range or some preprocessing) -> Double binary search
  |
  |
  else -> Basic binary search
