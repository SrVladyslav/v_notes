- Begin
  |
  |
  if (We pass the response from leaves to root\_) -> Bottom Up tree
  |
  |
  elif (Accumulates info from root to leaves) -> Up to bottom tree
  |
  |
  else -> Ask yourself questions about the life :)
  - Which is the best base case for the recursion and how to process it?
  - What we do before launching the left and right subtrees?
  - How we are processing the response
  - How many values are needed to be returned to the root and which ones? What they represent?
