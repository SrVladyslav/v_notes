- Begin
  |
  |
  if (find nearest greatest/smallest element) -> Monotonic stack
  |
  |
  elif (Stack can be replaced by one/various counter) -> Pseudo stack
  |
  |
  else -> Stack of intermediate results
