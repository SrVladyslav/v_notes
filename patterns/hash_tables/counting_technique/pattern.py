# Time: O(2n) -> O(n)
# Memory: O(k) where k is the number of unique chars


def is_palindrome_permutation(chars: list) -> bool:
    # =============================================================================
    # 1) Counter for every char in the string
    # =============================================================================
    count: dict[str, int] = {}

    for char in chars:
        # Counting every char frequency
        ...

    # =============================================================================
    # 2) Processing if the needed condition is possible
    # =============================================================================
    ...
