- Begin
  |
  |
  if (stores prefixes in plain text) -> if(sum order is important?) -> Classical Prefix sums. else: Prefix as a hash table key
  |
  |
  elif (We work with full sum/product accumulations on rows, cols, diagonals) -> Sum matrix
  |
  |
  else -> Running sum
