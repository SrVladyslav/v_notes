- Begin
  |
  |
  if (two lists of segments) -> pointer for everyone appraoch (two pointers n segments)
  |
  |
  elif (should find the max number of events in a moment of time) -> Dots method
  |
  |
  else -> Segments method
